"""
CodeAlchemist Agent Mode — backend package.

This package provides the clean, async agent runtime that powers
Agent Mode. It runs alongside the existing Flask app via FastAPI + ASGI.
"""

__version__ = "1.0.0"
